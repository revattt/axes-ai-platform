using Microsoft.AspNetCore.SignalR;

namespace Axes.AiService.Hubs // Change namespace if yours is different!
{
    // This is the radio tower. It doesn't need any code inside it right now.
    public class ProjectHub : Hub
    {
    }
}